# TODO: Реализовать функцию выделение фичей датасета
import pandas as pd

def process_data(orders: pd.DataFrame, purchases: pd.DataFrame, products: pd.DataFrame)-> pd.DataFrame:
    """
    Обрабатывает данные из трех DataFrame (orders, purchases, products)
    и возвращает обработанный DataFrame с выделенными признаками.

    Returns:
    - Результирующий DataFrame с выделенными признаками.
    """
    pass